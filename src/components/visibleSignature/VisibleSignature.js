import React, { Component } from 'react';

export default class VisibleSignature extends Component {
  render() {
    return (
      <section className="container">
        <h2>Visible signature</h2>
      </section>
    );
  }
}
