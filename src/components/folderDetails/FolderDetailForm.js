import React, { Component } from 'react';

export default class FolderDetailForm extends Component {
  render() {
    return (
      <form>
        <input type="text" />
        <textarea></textarea>
      </form>
    );
  }
}
