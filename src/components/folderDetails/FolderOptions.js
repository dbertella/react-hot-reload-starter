import React, { Component } from 'react';

export default class FolderOptions extends Component {
  render() {
    return (
      <form>
        <input type="text" />
        <textarea></textarea>
      </form>
    );
  }
}
